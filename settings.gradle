pluginManagement {
    repositories {
        mavenCentral()
        maven { url 'https://plugins.gradle.org/m2/' }
    }
}
rootProject.name = 'csense-kotlin-test'
enableFeaturePreview('GRADLE_METADATA')